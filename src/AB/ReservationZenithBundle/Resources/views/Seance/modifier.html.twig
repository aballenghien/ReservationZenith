{% extends "ABReservationZenithBundle::layout.html.twig" %}

{% block title %}ABReservationZenithBundle:Seance:modifier{% endblock %}

{% block body %}
<div class='well'>
	<h3>{{'modifier.seance'|trans}}</h3>
	{% if is_granted('ROLE_ADMIN') %}
	<form action='' method="post" {{form_enctype(form)}}>
		<table>
	    	<tr><td>{{form_label(form.heure)}}</td><td>{{form_widget(form.heure)}}</td></tr>
	    	<tr><td>{{form_label(form.date)}}</td><td>{{form_widget(form.date)}}</td></tr>
	    	<tr><td>{{form_label(form.nombrePlacesRestantes)}}</td><td>{{form_widget(form.nombrePlacesRestantes)}}</td></tr>
	    </table>    
		<div hidden="true"/>
		{{form_rest(form)}}
		<input type="submit" />
	</form>
	{% else %}
	<h1> {{'privilege.addsppectacle'|trans }}</h1>
{% endif %}
</div>
{% endblock %}
